{
  "name": "play-devcontainer",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "bun@1.3.0",
  "scripts": {
    "dev": "cd apps/web && bun run dev",
    "dev:api": "cd apps/api && bun run dev",
    "dev:web": "cd apps/web && bun run dev",
    "dev:all": "bun --workspaces dev",
    "start:api": "cd apps/api && bun run start",
    "start:web": "cd apps/web && bun run start",
    "test:run": "cd apps/api && npm run test",
    "test:watch": "cd apps/api && npm run test:watch",
    "db:migrate:dev": "cd packages/db && bun run db:migrate:dev",
    "db:migrate:reset": "cd packages/db && bun run db:migrate:reset",
    "db:migrate:deploy": "cd packages/db && bun run db:migrate:deploy",
    "db:seed": "cd packages/db && bun run db:seed",
    "db:studio": "cd packages/db && bun run db:studio",
    "db:generate": "cd packages/db && bun run db:generate",
    "clean": "bun --workspaces clean",
    "clean:api": "bun run --filter api clean",
    "clean:web": "bun run --filter web clean",
    "check:type": "bun --workspaces check:type",
    "build:ws": "bun run --workspaces build",
    "build:api": "cd apps/api && bun run build",
    "build:web": "cd apps/web && bun run build",
    "format": "biome format --write",
    "check": "bun run check:spell && bun run check:type && bun run check:biome",
    "check:biome": "biome check --write",
    "check:spell": "cspell lint . --gitignore --report=simple --dot",
    "prepare": "husky"
  },
  "dependencies": {
    "@biomejs/biome": "^2.2.5",
    "cspell": "^9.2.1",
    "husky": "^9.1.7"
  }
}
