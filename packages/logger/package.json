{
  "name": "@packages/logger",
  "version": "0.0.0",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "check:type": "tsgo --noEmit",
    "test": "bun test --watch",
    "clean": "echo 'No build artifacts to clean'",
    "build": "echo 'No build step needed (TypeScript source directly used)'"
  },
  "dependencies": {
    "pino": "^10.3.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "vitest": "^4.0.15"
  }
}
