{
  "name": "@packages/db",
  "version": "1.0.0",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "check:type": "tsgo --noEmit",
    "db:migrate:deploy": "bun run prisma -- migrate deploy",
    "db:migrate:dev": "bun run prisma -- migrate dev",
    "db:migrate:reset": "bun run prisma -- migrate reset --force",
    "db:seed": "bun run prisma -- db seed",
    "db:studio": "bun run prisma studio",
    "db:generate": "prisma generate",
    "db:generate:fabbrica": "prisma generate --generator fabbrica",
    "prisma": "bun run prisma",
    "clean": "rm -rf src/generated",
    "build": "echo 'No build step needed (TypeScript source directly used)'"
  },
  "packageManager": "bun@1.3.0",
  "dependencies": {
    "@packages/logger": "workspace:*",
    "@prisma/adapter-pg": "7.1.0",
    "@prisma/client": "7.1.0",
    "prisma": "^7.1.0"
  },
  "devDependencies": {
    "@quramy/prisma-fabbrica": "^2.3.3",
    "@types/bun": "latest"
  }
}
